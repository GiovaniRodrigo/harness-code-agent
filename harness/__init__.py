"""harness_ai — a minimal but real coding-agent harness.

The LLM is just one component. The harness provides the environment: context,
tools, state, limits (policies), a sandbox, and verifiable feedback (the
evaluator). The entry point is `harness.loop.run_agent`.
"""

__version__ = "0.1.0"
